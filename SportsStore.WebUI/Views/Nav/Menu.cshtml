@model IEnumerable<string>

                <li>
                    @Html.ActionLink(linkText: "Home", actionName: "List", controllerName: "Product")

            @foreach (var item in Model)
            {
                <li>
                    @Html.RouteLink(item, new { controller = "Product", action = "List", category = item, page = 1 })
                </li>
            }
