@model IEnumerable<string>

                
                    @Html.ActionLink(linkText: "Home", actionName: "List", controllerName: "Product",routeValues:null,htmlAttributes: new {@class="btn btn-button" })

            @foreach (var item in Model)
            {
                
                    @Html.RouteLink(item, new { controller = "Product", action = "List", category = item, page = 1 },new { @class="btn btn-button"})
                
            }
